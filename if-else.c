#include<stdio.h>
    void main(){
        int a,b;
        printf("enter the frist number: ");
        scanf("%d",&a);

        printf("enter the frist number: ");
        scanf("%d",&b);

        if(a,b){
            printf("the minimum value is:%d",a);
            } else {
             printf("the minimum value is:%d",b);
        }
        
        return 0;
        
    }
